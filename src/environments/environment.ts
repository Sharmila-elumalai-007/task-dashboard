export const environment = {
  production: false,
  // Vercel serverless function endpoint (keep secrets on the server)
  chatEndpoint: '/api/chat',

  // Model name your Vercel function should use
  openaiModel: 'openai/gpt-oss-120b:free'
};


